#include<iostream>
using namespace std;

int main()
{
    int first_number=0, second_number=0, sum=0;
    cout<<"Enter the values of two numbers, need to be added.";
    cin>>first_number>>second_number;

    sum=first_number+second_number;

    cout<<"Sum of both number is "<<sum<<endl;
    return 0;
}
